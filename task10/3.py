#round : aproximate to the nearest integer number if the decimal is less than 0.5 we aproximate down the number & if the decimal is >= 0.5 we aproximate up
# ceil : always aproximate up to the nearest integer
#floor : always aproximate down to the nearest integer